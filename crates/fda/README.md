# WingDrive FDA Handling

## `MacOS`

On `MacOS`, we are able to open the "Full disk access" settings prompt to instruct the user to allow WingDrive full disk access, which should alleviate all permissions issues.
