[package]
name = "sd-tauri-core"
version = "2.0.0-alpha.2"
edition = "2021"

[dependencies]
# Async runtime
tokio = { version = "1.40", features = ["full"] }

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Error handling
anyhow = "1.0"
thiserror = "1.0"

# Logging
tracing = "0.1"

# WingDrive path resolution and legacy adoption
sd-core = { path = "../../../core" }

[lib]
crate-type = ["lib"]

[build-dependencies]
